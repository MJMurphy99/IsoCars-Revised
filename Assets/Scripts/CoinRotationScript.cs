using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CoinRotationScript : MonoBehaviour
{
    // Update is called once per frame
    void Update()
    {
        //transform.Rotate(0, 10000* Time.deltaTime, 0);
    }
}
